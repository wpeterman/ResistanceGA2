% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Run_julia.R
\name{Run_CS.jl}
\alias{Run_CS.jl}
\title{Run Circuitscape via Julia}
\usage{
Run_CS.jl(
  jl.inputs = NULL,
  r,
  CurrentMap = FALSE,
  full.mat = FALSE,
  EXPORT.dir = NULL,
  output = "matrix",
  CS_Point.File = NULL,
  pairs_to_include = NULL,
  pop2ind = NULL,
  parallel = FALSE,
  cores = NULL,
  cholmod = TRUE,
  precision = FALSE,
  JULIA_HOME = NULL,
  rm.files = TRUE,
  scratch = NULL,
  is_resistance = TRUE
)
}
\arguments{
\item{jl.inputs}{Object from \code{\link{jl.prep}}. When provided, all other
Julia/CS settings are taken from this object.}

\item{r}{A \code{SpatRaster} (from \pkg{terra}) or path to a raster file
readable by \code{terra::rast()}.}

\item{CurrentMap}{Logical. Generate the cumulative current map?
Default = \code{FALSE}.}

\item{full.mat}{Logical. Return the full square distance matrix instead of
only the lower triangle? Default = \code{FALSE}. Cannot be requested when
\code{pairs_to_include} is used.}

\item{EXPORT.dir}{Directory where Circuitscape results are written. Required
when \code{CurrentMap = TRUE}.}

\item{output}{\code{"matrix"} (default) returns the lower-half pairwise
resistance vector; \code{"raster"} returns a \code{SpatRaster} of the
cumulative current map (requires \code{CurrentMap = TRUE}).}

\item{CS_Point.File}{Sample locations. Only needed when \code{jl.inputs} is
not provided. See \code{\link{jl.prep}}.}

\item{pairs_to_include}{1/0 vector selecting pairs. Only needed when
\code{jl.inputs} is not provided.}

\item{pop2ind}{Per-population sample-size vector. Only needed when
\code{jl.inputs} is not provided.}

\item{parallel}{Logical. Run Circuitscape in parallel? Default = \code{FALSE}.}

\item{cores}{Number of cores for parallel runs.}

\item{cholmod}{Logical. Use CHOLMOD solver? Default = \code{TRUE}.}

\item{precision}{Logical. Use single precision? Default = \code{FALSE}.}

\item{JULIA_HOME}{Path to the Julia \code{bin/} directory. Only needed when
\code{jl.inputs} is not provided.}

\item{rm.files}{Logical. Remove temporary files after the run?
Default = \code{TRUE}.}

\item{scratch}{Scratch directory for temporary files.}

\item{is_resistance}{Logical. Is \code{r} a resistance (vs conductance)
surface? Default = \code{TRUE}.}
}
\value{
Lower-half pairwise resistance vector, a full square matrix
  (\code{full.mat = TRUE}), or a \code{SpatRaster} current map
  (\code{output = "raster"}).
}
\description{
Executes Circuitscape through Julia (via \pkg{JuliaConnectoR}) and returns
pairwise resistance distances.
}
\examples{
\dontrun{
library(terra)
r         <- rast(nrows = 100, ncols = 100, vals = runif(1e4))
cs.result <- Run_CS.jl(jl.inputs = jl.inputs, r = r)
}
}
\author{
Bill Peterman <Peterman.73@osu.edu>
}
